version: 2

sources:
  - name: dashboard_mart
    tables:
      - name: dim_accounts
        config:
          external_location: "{{ var('data_root') }}/dashboard_mart/dim_accounts.parquet"
      - name: fact_transactions
        config:
          external_location: "{{ var('data_root') }}/dashboard_mart/fact_transactions.parquet"
      - name: dim_merchant
        config:
          external_location: "{{ var('data_root') }}/dashboard_mart/dim_merchant.parquet"
